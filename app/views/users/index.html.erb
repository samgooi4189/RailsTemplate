<% if user_signed_in? %>
<% else %>
	<div class="hero-unit">
    <h1>Alliarent: BE FREE on Renting</h1>
    <p>Easy, quick and we help you to get your security deposit back!</p>
    <p>
    <%= link_to("Signup!", new_user_registration_path, :class => "btn btn-primary btn-large") %>
    </p>
	 </div>
<% end %>
