<% if user_signed_in? %>
<% else %>
	<div class="hero-unit">
    <h1>Gmaker: Game maker on the web</h1>
    <p>No install, no hassle, and works for all!</p>
    <p>
    <%= link_to("Signup!", new_user_registration_path, :class => "btn btn-primary btn-large") %>
    </p>
	 </div>
<% end %>
